# 일기장 앱 단계별 구현 계획

## 1단계: 프로젝트 준비

- Next.js App Router 프로젝트를 생성한다.
- TypeScript를 적용한다.
- Tailwind CSS를 설정한다.
- shadcn/ui를 설치하고 설정한다.
- Supabase 프로젝트를 생성한다.
- `.env.local`에 Supabase 환경변수를 추가한다.

필요한 환경변수:

```txt
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

## 2단계: Supabase 스키마 설정

- `profiles` 테이블을 만든다.
- `diaries` 테이블을 만든다.
- `diary_images` 테이블을 만든다.
- `diary_share_links` 테이블을 만든다.
- `diary-images` Storage 버킷을 만든다.
- 사용자 소유 데이터가 있는 테이블에 RLS를 활성화한다.

핵심 테이블:

```txt
profiles
diaries
diary_images
diary_share_links
```

## 3단계: 인증 기능

- 회원가입 페이지를 만든다.
- 로그인 페이지를 만든다.
- 로그아웃 기능을 추가한다.
- 로그인한 사용자만 접근할 수 있는 페이지를 보호한다.
- 로그인한 사용자는 일기 목록으로 이동시킨다.
- 로그인하지 않은 사용자는 인증 페이지로 이동시킨다.

추천 라우트:

```txt
/login
/signup
/diaries
```

## 4단계: 기본 일기 CRUD

- 새 일기를 작성할 수 있게 한다.
- 현재 사용자의 일기 목록을 보여준다.
- 일기 상세 화면을 만든다.
- 일기를 수정할 수 있게 한다.
- 일기를 삭제할 수 있게 한다.
- 사용자가 자기 일기만 접근할 수 있게 한다.

추천 라우트:

```txt
/diaries
/diaries/new
/diaries/[id]
/diaries/[id]/edit
```

## 5단계: 일기 입력 필드 확장

일기 작성 폼에 아래 항목을 추가한다.

- 제목
- 본문
- 작성 날짜
- 감정
- 날씨
- 태그
- 즐겨찾기 여부
- 템플릿 종류
- 템플릿별 입력 데이터

`template_data`는 JSON 형태로 저장한다. 이렇게 하면 템플릿마다 다른 입력 항목을 유연하게 저장할 수 있다.

## 6단계: 글쓰기 템플릿

처음에는 네 가지 템플릿으로 시작한다.

- 자유 일기
- 하루 회고
- 감정 기록
- 감사 일기

템플릿 입력 예시:

```txt
하루 회고
- 오늘 있었던 일
- 좋았던 일
- 아쉬웠던 일
- 내일 하고 싶은 일

감정 기록
- 오늘의 감정
- 감정의 이유
- 나를 도와준 것
- 지금 필요한 것

감사 일기
- 감사한 일 1
- 감사한 일 2
- 감사한 일 3
- 오늘의 한 줄
```

## 7단계: 사진 첨부

- 일기 작성 페이지에 이미지 업로드를 추가한다.
- 일기 수정 페이지에 이미지 업로드를 추가한다.
- 이미지 파일은 Supabase Storage에 저장한다.
- 이미지 메타데이터는 `diary_images`에 저장한다.
- 일기 상세 화면에서 이미지를 보여준다.
- 이미지 삭제 기능을 추가한다.

추천 Storage 버킷:

```txt
diary-images
```

추천 저장 경로:

```txt
{user_id}/{diary_id}/{image_id}.{extension}
```

## 8단계: 날짜별 보기

- 달력 또는 날짜 필터 페이지를 만든다.
- 선택한 날짜의 일기를 보여준다.
- 사용자가 이전 달과 다음 달로 이동할 수 있게 한다.
- MVP에서는 완성된 달력 UI보다 간단한 날짜 필터로 먼저 시작해도 좋다.

추천 라우트:

```txt
/calendar
```

## 9단계: 공유 링크

- 일기 상세 화면에 공유 링크 생성 버튼을 추가한다.
- 랜덤 토큰을 생성한다.
- 토큰을 `diary_share_links`에 저장한다.
- 공개 읽기 전용 공유 페이지를 만든다.
- 작성자가 공유 링크를 비활성화할 수 있게 한다.

추천 공개 라우트:

```txt
/share/[token]
```

공유 동작:

- 링크를 가진 사람은 일기를 읽을 수 있다.
- 공개 방문자는 로그인하지 않아도 된다.
- 공개 방문자는 일기를 수정하거나 삭제할 수 없다.
- 비활성화된 링크는 일기를 보여주지 않는다.

## 10단계: UI 정리

- 모바일 화면을 최적화한다.
- 로딩 상태를 추가한다.
- 빈 상태 화면을 추가한다.
- 에러 메시지를 추가한다.
- 글쓰기 화면은 집중하기 쉽게 만든다.
- 전체 디자인은 차분하고 미니멀한 일기장 느낌으로 정리한다.

중점적으로 다듬을 화면:

```txt
인증 페이지
일기 목록
일기 작성/수정 화면
일기 상세
날짜별 보기
공개 공유 페이지
```

## 11단계: 테스트와 배포

- 사용자가 다른 사용자의 일기를 읽을 수 없는지 확인한다.
- 사용자가 다른 사용자의 일기를 수정하거나 삭제할 수 없는지 확인한다.
- 이미지 업로드가 동작하는지 확인한다.
- 이미지 삭제가 동작하는지 확인한다.
- 공유 링크가 로그인 없이 동작하는지 확인한다.
- 비활성화된 공유 링크가 차단되는지 확인한다.
- Vercel에 배포한다.
- Vercel에 Supabase 환경변수를 설정한다.

## 추천 빌드 순서

```txt
프로젝트 준비
-> Supabase 스키마와 RLS
-> 인증
-> 일기 CRUD
-> 템플릿
-> 사진 첨부
-> 날짜별 보기
-> 공유 링크
-> UI 정리
-> 테스트와 배포
```
